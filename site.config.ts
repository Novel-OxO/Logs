import { siteConfig } from './lib/site-config';

export default siteConfig({
  // 사이트 기본 정보 (필수)
  name: 'Dev Log',
  domain: 'localhost:3000',
  author: 'Novel',

  // OpenGraph 메타데이터 (선택)
  description: 'Notion으로 만드는 개발 블로그',

  // 소셜 계정 (선택)
  // twitter: '',
  // github: '',
  // linkedin: '',
});
